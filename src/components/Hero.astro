---
import Button from './Button.astro';
import { asset } from '../lib/assets';
import { t, type Locale } from '../lib/i18n';
import { urlFor } from '../lib/sanityImage';

interface HeroImage {
  asset?: { _ref?: string; _id?: string } | null;
  alt?: string | null;
  hotspot?: unknown;
  crop?: unknown;
}

interface Props {
  locale?: Locale;
  headline?: string | null;
  body?: string | null;
  ctaLabel?: string | null;
  image?: HeroImage | null;
}

const CTA_HREF = '#quote';

const {
  locale = 'en',
  headline,
  body,
  ctaLabel,
  image,
} = Astro.props;

const imageSrc = image?.asset
  ? urlFor(image).width(2400).quality(80).auto('format').url()
  : asset('/counter-display/1-03.webp');
const imageAlt = image?.alt || t(locale, 'hero.imageAlt');
const title = headline || t(locale, 'hero.headline');
const description = body || t(locale, 'hero.body');
const buttonLabel = ctaLabel || t(locale, 'hero.ctaLabel');
---

<section
  class="relative flex min-h-[calc(100svh-var(--nav-height))] items-center justify-center overflow-hidden"
  aria-label={t(locale, 'hero.ariaLabel')}
>
  <img
    src={imageSrc}
    alt={imageAlt}
    width="1920"
    height="1080"
    class="absolute inset-0 size-full object-cover"
    loading="eager"
    decoding="async"
    fetchpriority="high"
  />
  <div class="absolute inset-0 bg-mid-dark/70" aria-hidden="true"></div>

  <div
    class="relative z-10 mx-auto flex max-w-container flex-col items-center px-[6vw] py-16 text-center text-white" data-stagger-children
  >
    <h1 class="font-heading text-[5rem] font-medium tracking-tight max-lg:text-4xl max-md:text-[3rem]">
      {title}
    </h1>
    <p class="mt-4 max-w-2xl text-lg text-white/95 max-md:text-base">{description}</p>
    <Button href={CTA_HREF} label={buttonLabel} class="mt-8" />
  </div>
</section>
